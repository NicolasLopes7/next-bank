import { extendTheme } from '@chakra-ui/react';

const colors = {
  background: '#f6f0e6',
  white: '#fff',
  darkText: '#3c4151',
  lightText: '#aabccf',
  primary: '#6371de',
};

export const theme = extendTheme({ colors });
